export class Canal {
    constructor(nombre, baner, descripcion, streamer, streams) {
        this.nombre = nombre;
        this.baner = baner;
        this.descripcion = descripcion;
        this.streamer = streamer;
        this.streams = streams;
    }
}
